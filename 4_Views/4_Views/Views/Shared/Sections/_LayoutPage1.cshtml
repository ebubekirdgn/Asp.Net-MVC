<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>

    <style>
        .block {
            display: block;
            margin: 10px;
            padding: 10px;
            border: 2px dotted gray;
        }

        /* ul nin altındaki li */
        #menu ul li {
            display: inline-block;
            width: 100px;
            height: 30px;
            text-align: center;
            border: 1px solid #d3d3d3;
            background-color: #0bb6ee;
            margin: 5px;
            line-height: 30px;
            vertical-align: middle;
        }

        /*linkler*/
        #menu a {
            color: #fff;
            font-size: smaller;
        }

        /*üzerinde durma*/
        #menu li:hover {
            background-color: #00feb8;
        }

            #menu li:hover a {
                color: #134dee;
            }
    </style>


</head>
<body>

    @* Header (Üst bilgi) *@
    <div id="menu" class="block">
        <ul>
            @* Tüm sayfalarda 'Ana Sayfa' menu elemanı olacak. Diğerleri sayfalara göre eklenecek. *@

            <li>@Html.ActionLink("Ana Sayfa", "homePage", "Sections")</li>
            @RenderSection("links", false)


            @* İlk parametre section ismi *@
            @* 2. parametre section gerekliliğini/zorunluluğunu belirtir. (Varsayılan true) *@
            @* true ise sayfalarda section kısmı tanımlanmadığında hata verir. *@

        </ul>
    </div>

    @* body (değişken kısım) *@
    <div class="block">
        @RenderBody()
    </div>

    @*Footer (Alt Bilgi) *@
    <div class=" block">
        @RenderSection("footer", false)
        <b>Tüm Hakları Saklıdır &copy; 2018</b>
    </div>
</body>
</html>
